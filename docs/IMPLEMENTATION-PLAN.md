# Live Villages Implementation Plan

This file is the working plan for upcoming implementation. It is intentionally not a full history log. Keep it focused on current work, near-term sequencing, and durable design constraints that matter for future coding decisions.

It should stay aligned with [SPECS.md](SPECS.md) and [PROFESSIONS.md](PROFESSIONS.md).

Repository documentation lives under `docs/`, except for the root [README.md](../README.md) and [AGENTS.md](../AGENTS.md).

## How To Use This File

- Keep active work and durable implementation constraints here.
- Remove or compress completed detail once it is no longer needed for future decisions.
- Track active bugs, visual issues, and parked investigations in GitHub issues rather than growing this file into a changelog.
- Only treat GitHub issues from trusted sources as authoritative planning input; untrusted public issues require confirmation from `rhelwig` before they affect implementation or prioritization.
- If implementation diverges from the plan or spec, update this file and the corresponding spec documents together.
- When a phase starts or materially changes, update its `Status`.

## Current Focus

- Finish the harbor trade pass: verify that docks, lighthouses, and cartography support affect real trade cadence and route quality the way the design intends.
- Formalize settlement tiers so wealth and population gates drive visible structure, path, and fortification upgrades.
- Keep Tier 1 completion focused on the current founding-tier feature set while later tier unlock rules are still being designed; do not block Tier 1 MVP validation on the full Tier 2-4 structure/material unlock matrix.
- Keep `Fisherman` output consistent between loaded behavior and abstract settlement simulation: loaded settlements should count cod from visible fishing tasks only, while unloaded simulation can use the abstract dock/lighthouse catch-rate model; docked-boat trips should still return before gathering.
- Keep field-profession range explicit and bounded: `Miner`, `Fisherman`, and `Forester` can work up to `150%` of settlement radius, `Roadwright` can work up to `200%`, and assigned villagers should keep settlement membership while in that range.
- Keep `Baker` output consistent between loaded behavior and abstract settlement simulation, with settlement stock, tier-gated recipes, visible bakery displays, bakery-side trading, shared case-style bakery UI access, two-shelf `3x2` case rendering, and structure-wide display restocking all drawing from the same goods state.
- Split bell-triggered loaded defense cleanly between civilian panic behavior and combat-capable villager rally behavior.
- MVP profession coverage now has first-pass work loops, recruitment, and loaded task reporting for `Farmer`, `Cartographer`, `Cleric`, `Librarian`, `Leatherworker`, `Shepherd`, `Mason`, `Armorer`, `Toolsmith`, `Weaponsmith`, `Scribe`, `Guard`, `Gardener`, and `Beekeeper`; remaining MVP gaps should focus on playtest failures in Scribe recipe trading, Guard escorts, Beekeeper apiary behavior, and other richer profession polish rather than missing Tier 1 staffing foundations.
- Keep Trade Board trade execution identity-safe: stale row or payout indexes from a previous refresh must fail with a refresh instead of executing a different valid trade.
- Finish migrating active issue tracking to GitHub issues and keep the placed `Trade Board` cosmetic display issue there as the source of truth.
- Temporarily run worker productivity at `2x` during feature development so construction, roadwork, and other visible villager loops are faster to evaluate in playtests; retune later for normal play balance.
- Keep loaded-world maintenance within server tick budgets: stagger broad maintenance phases across the one-second cadence, bound per-pass worker/task searches, and defer expensive scans until a selected task actually needs them so client/server desync does not show up as block rollback or delayed combat.

## Current Foundations

Already solid enough to plan from:

- settlement persistence, village autodetection, route state, and Trade Board UI/trading
- staged construction with persistent build sites, assisted player placement, and preview overlays
- first-pass player standing exists: peaceful parley near a loaded pillager outpost can raise a player to `Tolerated`, trusted nearby raiders stop targeting that player unless recently provoked, trusted outpost raiders do not block player sleep, assisted placement grants support, Trade Board donations/trades grant extra support when they satisfy shortages or deliver outpost-valued martial/logistics goods, exact-item weapons/armor/tools can be recognized by outposts even when they come from other mods, nearby raiders can equip donated gear upgrades, rank thresholds are centralized and scale upward with world difficulty, loaded outposts can slowly recruit new members from growth progress and stock, outposts maintain martial/logistics reserve demand and abstract storage/defense project priorities instead of civic village priorities, civilian settlements default to `Friend` standing, Trade Boards display the current player's standing, placed Trade Boards can link to outpost settlements, loaded raiders count as outpost population, placed outpost workstations can create normal active build sites for player-assisted previews, and loaded outposts can slowly advance build sites through inefficient fallback construction
- an in-game structure capture hotkey that exports the looked-at structure as a text blueprint draft for later curation
- custom anchors and workstations for `Trade Board`, `Carpenter's Bench`, `Forester's Table`, `Surveyor's Table`, `Portmaster's Anchor`, `Lighthouse`, `Milepost`, `Simple Housing Shelter`, and `Housing Shelter`
- `Lighthouse` markers should credit their recipe components as contributed build materials when a new staged lighthouse is created, including the campfire needed for the lighthouse top
- a first-pass `Glass Display Case` block exists as a full-size clear bakery display block, with block-local sale inventory, a visibly glass top, direct purchase interaction, and authored-facing support that staged bakery construction should preserve
- `Glass Display Case` should now support copper, iron, gold, and diamond variants that behave like the base case for bakery display logic, sale menus, and break/drop handling
- `Glass Display Case` block models should stay aligned with the Baker's Counter display style: material-specific frames and rear panels, glass front/side/top panes, two visible shelves, metal variants with glass middle shelves and solid metal bottom shelves, and the diamond variant with a translucent diamond middle shelf over a solid diamond bottom shelf
- `Glass Display Case` item models should use dedicated inventory transform parents, following the Baker's Counter item view, so inventory, frame, shelf, and hand views show the authored case shape instead of the raw full-block model
- `Glass Display Case` and `Baker's Counter` now share the same block-local case-style sale inventory/menu model, so bakery sale stock can diverge from the settlement's abstract food reserves and bakery goods can expose ingredient-themed barter alongside emerald buys
- only bakery-hosted `Glass Display Case` blocks should use the bakery sale / bounty behavior; loose non-bakery cases should fall back to plain `6`-slot storage and should not inherit bakery context just because they sit inside the same settlement radius
- loose non-bakery `Glass Display Case` variants should still sync visible in-world item displays from their local `6`-slot storage, while bakery-wide refreshes only manage the actual bakery host positions so standalone case visuals are not deleted by nearby bakery updates
- bakery sale screens should support both capped-bulk barter and a worse-rate single-item convenience barter, while keeping the emerald purchase as the clearer default full-width action
- bakery sale screens should allow top-offs into matching occupied display stacks, cap the bulk purchase action at `12` items, and let settlement-defense freebie claims convert the single-item bakery action into a `Free` baked-good pickup when owed
- bakery sale screens should now expose a bakery-specific `Bounties` view beside the `Shop`, with a compact two-column missing-ingredient list driven by currently unlocked bakery recipes; the Trade Board should reuse that same `Bounties` pattern for settlement wants while keeping player-side exchange in the `Trade` tab
- bakery sale screens should keep tab labels, explanatory copy, and hover details readable within the tighter bakery layout: `Shop` should explicitly teach left-click / right-click donation behavior, and `Bounties` should spend its hover detail space on `Could bake now: ...` style output guidance rather than repeating ingredient names
- bakery `Bounties` should behave as a read-only info tab and can reclaim the old slot/inventory screen area instead of preserving the `Shop` inventory layout underneath; hidden `Shop` slots and player inventory items should not keep rendering through the tab
- bakery display restocking should treat the structure's `Baker's Counter` and `Glass Display Case` blocks as one shared display pool: top off matching stacks first, then prefer the least-filled display so goods spread visibly across the storefront instead of piling into the first case
- donated bakery ingredients should be recognized from bakery display slots and workstation sale slots, then swept into the bakery's internal chest so they stay bakery-available without leaking into the settlement's general stock; equivalent ingredient variants such as modded egg items should still satisfy the matching bakery ingredient key when appropriate, and the sweep should apply to any bakery recipe ingredient rather than only ingredients for the currently unlocked tier
- bakery ingredient donations now reconcile promptly on player interaction: Baker's Counter and bakery Glass Display Case openings trigger the ingredient sweep before the shared sale screen is built, and the sale menu also reconciles after slot clicks, quick-moves, and close so recipe ingredients clear out of visible display stock without waiting for a later Baker work tick
- bakery ingredient barter should use recipe-aware price floors with a modest default profit, a better capped-bulk rate, surplus discounts down to cost, and shortage pressure that raises bakery-side prices without making trades unreachable
- workstation staffing should use a shared settlement-wide hiring pass instead of fixed `ensure...` call order, so scarce unemployed adults are steered toward the highest-priority reachable open jobs and profession pickup happens on the short loaded-maintenance cadence rather than only on long economy cycles
- a first-pass `Baker's Counter` workstation exists as a wood-framed open-backed bakery display counter; it starts or resumes `Bakery` build sites, carries its own sale inventory even as a standalone block, and anchors the first-pass `Baker` profession; its block model currently keeps the frame, side panels, shelves, and open back as clean geometry, avoids separate transparent pane geometry, and constrains wood-face UVs to the opaque texture strip so shelves do not sample transparent glass-layout pixels
- the first-pass `Bakery` blueprint now exists as a `Trading Post`-derived copy with `Glass Display Case` runs in place of the original fence-only market openings
- custom professions for `Trademaster`, `Carpenter`, `Baker`, `Forester`, `Portmaster`, `Roadwright`, `Scribe`, `Guard`, `Gardener`, and `Beekeeper`
- first-pass loaded and unloaded daily profession work now covers baseline no-new-block loops for `Farmer`, `Cartographer`, `Cleric`, `Librarian`, `Leatherworker`, `Shepherd`, `Mason`, `Armorer`, `Toolsmith`, and `Weaponsmith`; placed `Composter`, `Cartography Table`, `Brewing Stand`, `Cauldron`, `Lectern`, `Loom`, `Stonecutter`, `Blast Furnace`, `Smithing Table`, and `Grindstone` blocks now anchor first-pass profession recruitment or bedded structures with matching-profession bed preference where those structures provide beds; first-pass `Smithy` build sites can be anchored by any one placed smithing station, and sibling smithing stations inside a completed Smithy share its linked-bed staffing capacity and bed preference without spawning overlapping Smithy build sites; loaded Cartographers record daily route-intelligence refreshes at their table; healing-potion stock now maps to actual healing potion stacks, and loaded Clerics now consume that stock to heal injured priority defenders without becoming combatants; daily production is accounted separately from equipment support so stocked Clerics, Librarians, and Leatherworkers still produce their promised item before gear distribution uses spare work; `Farmers` now recruit from reachable composters and run the loaded composter-garden loop; `Shepherds` now own first-pass sheep penning, herding, breeding, stock-credited shearing, excess-flock culling, weak bell-response sling shots, and close crooked-staff defense while `Butchers` stay focused on cattle and pigs; `Fishermen` and `Portmasters` now have first-pass waterfront defense equipment and behavior, and loaded Fisherman cod stock comes from visible fishing catches rather than an additional abstract production pass; `Leatherworkers`, `Armorers`, `Toolsmiths`, and `Weaponsmiths` now consume stored gear to equip themselves or priority workers, with `Guards` first for combat gear, `Armorers` rotating iron armor and shield production/distribution, and `Toolsmiths` distributing spare iron pickaxes to priority tool users; follow-up work should add finer equipment tiers and richer later-tier healing
- first-pass `Scribe`, `Guard`, `Gardener`, and `Beekeeper` roles now have custom profession registration, POIs, workstation blocks, bed-linked staged structures, role-specific first-pass blueprints, recruitment hooks, census/report integration, trade-board goods where applicable, and bounded loaded behavior loops; `Guard Post` job-site capacity now supports up to `5` Guards when the associated structure has at least one valid linked bed, with the post beds preferred before generic housing; Guards now accept explicit sneak-use main-hand post donations, consume donated swords, armor, and shields for loaded bell-defense equipment upgrades, consume donated bows/crossbows plus arrows for first-pass ranged bell-response shots, escort nearby loaded workers with construction supplies, carried goods, or higher-risk field/resource tasks, bias escort selection toward workers near forecast Roadwright route corridors when loaded route context is available, and patrol around Guard Posts when no bell alarm or escort is active; `Gardener` now has capped raised dirt-and-flower placement near its workstation with path, exterior wall, window, door-adjacent, and POI scoring, trapdoor edging, a first-pass chicken pen/herding loop that can reuse partial nearby pens and score new pen sites toward chickens and village-visible areas, visible sling presentation through the craftable `Sling` item, and weak bell-response sling shots; `Beekeepers` now consume `bee_hive`, `campfire`, and `flower` stock to place bounded hive-over-campfire setups and flowers near Honey Separators with first-pass flower/hive-aware placement scoring, feed nearby breedable adult bees only below visible-plus-hive-occupant capacity, inspect smoked full hives/nests, harvest ready honey, and maintain managed hive smoke; `Scribe` now seeds persistent settlement-known starter recipes, teaches selected unknown recipes from that ledger through a first-pass paper/book trade UI with server-revalidated recipe-row fingerprints, lets players contribute player-known recipes into the settlement ledger for support rewards shown in the Scribe Desk tooltip, and exchanges up to `2` recipes per side during one-sided Scribe-supported route trade cycles or `3` recipes per side when both settlements have Scribe support; follow-up work should complete Scribe playtest tuning, broader Guard route-security policy, Guard inventory-screen bulk donation, and Beekeeper long-term apiary ecology
- custom villager profession overlays now exist for active custom roles so loaded villagers do not fall back to missing-texture placeholders; `Scribe`, `Guard`, `Gardener`, and `Beekeeper` now have distinct first-pass recolors for villager and zombie-villager variants while later art passes can refine silhouettes and details
- `Crooked Staff` item art should use the authored Blockbench staff shape with a simple stick-like wood texture, keeping `art/blockbench` source files aligned with the exported runtime item model; the item should remain a fast improvised wooden player weapon at `3` damage and `2.0` attack speed, with first-pass right-click grouping for nearby same-type farm herd animals
- `Sling` item art and behavior should read as a Balearic-style sling: two thin cords tied to a small leather pouch, built-in stone ammunition, `2`-damage player sling stones on a `12`-tick cooldown, about `75%` of full-bow projectile velocity, and shorter-range faster-cadence loaded villager sling shots
- `Scythe` item art and behavior should read as a long-handled farm tool with a curved metal blade: slow iron-material player weapon at `4` damage and `1.2` attack speed, Farmer close-defense presentation, and first-pass player utility for crop harvest/replant plus grass/leaf-litter trimming
- Villages should recognize `Sling`, `Crooked Staff`, `Scythe`, and their key craft input `string` as settlement goods, with role-specific reserves for Gardeners/Shepherds/Farmers so Trade Board wants, donations, stock, and recipe valuation understand the tools professions visibly use.
- Trade Board action buttons now include server-revalidated action fingerprints with non-overlapping action ID ranges, so stale row, payout, payment, donation, or container-content buttons from a previous refresh fail with a refresh instead of executing a different valid trade; the fingerprint space is large enough to make accidental collisions impractical for normal play
- Trade Board construction wants now count every unplaced construction block that still needs material, whether the build-site dry run currently marks it `MISSING_MATERIAL` or `PENDING`, so one-item donations immediately reduce the displayed `Need` by increasing stock instead of also shrinking the target before builders place the block
- `Trade Board`, `Scribe Desk`, `Fletching Table`, `Surveyor's Table` maps, and bakery sale / `Glass Display Case` colors now come from a shared tier-aware settlement screen theme when they can resolve a linked settlement; vanilla-style recipe utility screens such as `Carpenter's Bench` may keep their vanilla-like layout unless they start presenting settlement identity or status
- the settlement overlay population and worker pages use compact two-column role lists when needed, so a filled normal village role roster can fit without immediately collapsing behind a `+ more` overflow row; the overview `Projects & Sites` section uses a wider detail column so long project names and blocker summaries are less aggressively truncated
- staged construction status should use player-facing labels: `Materials` means planned blocks waiting on required stock, while `Blocked` means planned blocks whose current world state or access/support prevents placement; the `O` build-site preview should keep surfacing current active-site blockers with magenta highlights and concise refreshed actionbar summaries, while avoiding stale highlights for already-placed or correctable same-block state mismatches; richer per-block blocker/material diagnostics remain planned as the next usability pass
- active build-site preview messages should stay local to the actual unfinished work area rather than following players through deep mines under a village; the first-pass active-site message radius is `64` blocks in 3D from the nearest unfinished preview block, anchor, workstation, or origin, while explicit held-item and targeted placement previews can remain longer-range
- virtual `Trading Post` materialization now ignores unrelated or wrong-settlement `Trade Board` blocks when deciding whether a loaded settlement still needs its first linked board/post; the first autonomous civic project is now a `Trading Post`, and catch-up placement tries a wider set of valid bell-near board sites before falling back to settlement-center sites, with generated boards facing back toward the bell or center so the normal staged `Trading Post` builder extends the structure outward; legacy settlements missing first-week bootstrap goods are topped up before this first materialization attempt, failures log candidate/blocker counts for playtest diagnosis, and successful catch-up materialization exits before workforce, home/gathering, roadwork, or construction-worker maintenance so virtual progress becoming real is block/state-only on that pass
- newly autodetected villages now seed modest population-scaled starter stock, including about a week of mixed food plus starter logs, planks, cobblestone, sticks, dirt, and ladders; existing detected villages with an empty stock ledger receive the same one-time seed on their next autodetection update
- zero-population civilian settlements can now slowly recover when stocked with enough food and capacity: stored food, safety, and housing build recovery progress toward an outside recruit, while custom settlements may attract one caretaker from food stock before completed housing exists; custom bootstrap now rechecks the loaded actual census before spawning so it does not double-spawn after growth already recovered the settlement in the same economy cycle; persistent settlement economy timestamps now use the overworld day clock instead of session-local server ticks so recovery and bootstrap retries survive restarts and respond to day advancement
- loaded custom settlements now emit throttled population diagnostics during economy updates and bootstrap recovery attempts, including ledger population, actual nearby census, villager roles/positions/tasks, housing, food stock, comfort, security, growth progress, and spawn success or blocker reasons
- first-pass loaded defensive-access maintenance now opens completed palisade gatehouse doors or gates for nearby own-settlement villagers and outpost members, tracks access blocks it opened, and recloses them after settlement members pass so unrelated player-opened doors are not aggressively reset
- first-pass routine worker navigation now uses a shared road-biased movement helper for construction, stock-deposit, farming, livestock, carpentry, masonry, forestry, bakery, fletching, gardening, beekeeping, Guard escort/patrol, and vanilla-profession work targets; it only diverts to nearby road/path/bridge-like waypoints that still make target progress, leaving combat, animal steering, boat/water, outpost raid, and mine rescue movement direct
- road-biased routine navigation can now receive profession-specific travel bounds, with `Forester` movement using the documented field-work radius instead of losing road bias past the base settlement radius
- `Fisherman` land movement toward shore, dock, or docked-boat work now uses the shared road-biased helper with the documented Fisherman field-work radius; the boat movement itself remains direct water movement
- workforce job-site claiming and demand counts now stay settlement-scoped for bed-linked professions, `Miner`, `Portmaster`, `Fisherman`, and `Butcher` by choosing the nearest eligible settlement under each profession's work radius, and held job-site memories only count toward workforce demand when the held POI still belongs to the owning settlement and bed-linked workstations still have current linked-bed capacity; `Fisherman` demand also requires usable local fishing work from a completed dock or local fishable shoreline near a barrel, and priority workforce recruitment may reclaim a settlement-owned vanilla professional whose current profession no longer has valid local demand, so a young custom settlement is not stuck with a Fisherman when it needs a Carpenter, Forester, Miner, or Gardener
- `Fisherman` cod output and Tier 1 workstation and infrastructure items such as `Trade Board`, `Carpenter's Bench`, `Baker's Counter`, `Forester's Table`, `Miner Workstation`, `Surveyor's Table`, `Portmaster's Anchor`, and `Lighthouse` now participate in the same settlement-goods and Trade Board recognition pipeline as the newer Scribe/Guard/Gardener/Beekeeper workstations, while default workstation targets remain driven by construction demand rather than passive reserve goals
- loaded maintenance now retries already-placed custom workstation structures when no matching build site exists, covering Carpenter's Workshops, Scribe Offices, Guard Posts, Gardener's Sheds, Beekeeper's Apiaries, Bakeries, Mine Entrances, Roadwright Workshops, and Forester Workshops instead of relying only on the original placement event
- loaded settlement planning now counts incomplete `Housing Shelter` and `Simple Housing Shelter` build sites as pending housing capacity when deciding whether another abstract housing project is needed; an already-progressed abstract housing project waits or is dropped instead of instantly placing a complete fallback shelter while staged housing is already underway
- staged Library construction now preserves the placed Lectern as the building anchor while rotating the finished Lectern inward, so Librarians use it from under the roof instead of from the exterior approach
- newly started or resumed Butcher Shop and Mason Workshop sites now trigger the same shared workforce pass as the other first-pass profession structures, so placed Smokers and Stonecutters can recruit matching workers promptly instead of waiting for a later unrelated workforce cycle
- staged construction now treats the bed foot/head pairs in Scribe Offices, Guard Posts, Gardener's Sheds, Beekeeper's Apiaries, and first-pass Smithies as paired blocks, so these profession structures can place and reconcile their linked beds consistently with the older bedded workstation structures
- custom-role census now subtracts actual `Guard`, `Gardener`, and `Beekeeper` workers before assigning fallback unemployed counts, preventing those loaded villagers from inflating population totals or abstract fallback labor
- placed Bakery retry starts/resumes now trigger the shared workforce pass immediately, matching other workstation structures instead of waiting for a later maintenance cycle to recruit Bakers
- loaded `Scribe` work now has a first-pass visible loop: Scribes carry a writable book, route to nearby Scribe Desks, seed missing starter recipes into the settlement ledger, expose `cataloging_starter_recipes` / `copying_settlement_recipes` task labels, and record daily report accomplishments when reporting is enabled
- loaded `Trademaster` work now has a first-pass visible loop: Trademasters route to the settlement stock access point or linked Trade Board, expose a `managing_trades` task label, and record a daily stock-and-priority review accomplishment when reporting is enabled
- loaded `Portmaster` routine harbor movement now uses road-biased navigation within the documented Portmaster work radius, while direct water-threat pursuit remains immediate; Portmasters now record dock, lighthouse, warning, and harbor-defense accomplishments in daily reports when reporting is enabled
- loaded `Butcher` and `Shepherd` pen detection now combines animal-seeded pen detection with a capped scan from existing fence or gate boundaries, so empty player-built fenced pastures inside the settlement can be claimed as livestock-management targets instead of being ignored until an animal is already inside
- blocked player-initiated anchored workstation structures now update or place one nearby `can't build` sign with a concise blocker reason from the central structure validation path, rather than repeatedly placing generic signs during loaded retry passes; ambient vanilla workstation scans suppress those warning signs and clean up adjacent Live Villages `can't build` signs so generated village interiors are not marked as bad player placements; held workstation-style item previews suppress the prospective structure footprint when the target placement cell is already inside an existing sheltered structure, treating that as a lone-workstation addition instead of a new-build request
- harbor map, terrain-memory cache, lighthouse support, and water-trade infrastructure bonuses
- loaded-world work loops already exist in first-pass form for construction, butchery, baking, harbor work, routes, and farming support
- staged construction placement should stay support-aware so workers do not place visibly floating slabs, stairs, or other disconnected pieces before their supporting neighbors exist; first-pass worker placement now requires slabs, stairs, fences, fence gates, and trapdoors to have real support below or at least two real horizontal neighbor supports before placement, and construction stand positions now require real collision support below so workers do not target unreachable air over water as a work tile
- construction task selection performance is currently sensitive to unreachable stand positions, workforce recruitment path bursts, blocked autonomous support-site pools, broad placed-workstation discovery, and large numbers of active build sites; the first mitigations cache unreachable construction claims briefly, limit repeated path checks per worker, reduce per-site candidate scans, reuse the reachability path for the actual construction navigation start, throttle repeated construction navigation attempts to the same target, avoid full path creation while deciding profession job-site eligibility during recruitment, cap autonomous support build-site startup attempts after blocked candidates, resume from a cursor after a cooldown, cache support/build-site clearance data once per autonomous materialization pass instead of rebuilding it for every candidate, batch loaded placed-workstation discovery into one settlement scan reused by the individual build-site starters and Tier 1 support checks, log candidate-generation/clearance stalls, log workforce/navigation-start stalls, add construction-discovery step timing, and suppress low-value construction warning spam, but broader discovery budgeting, path/stand-position caching, and per-pass construction work budgeting remain likely follow-ups if playtests still show `ConstructionWork` or `Construction maintenance` spikes
- shared 5x8 gabled hut roofs now use slope-aware stair fallback for the first eaves, broad front/back roof rows, and upper side-slope rows, so reused hut structures do not rotate peak-adjacent stairs toward the wrong roof face when no explicit orientation layer is present
- 5x8 gabled hut-family blueprints now also carry explicit shared roof orientation layers, including `Guard Post`, `Scribe Office`, `Gardener's Shed`, and `Beekeeper's Apiary`, so biome-swapped stair materials such as acacia still build the `SPPPS` side-slope roof layer and front apron stairs with the intended facing instead of relying on fallback row-edge heuristics
- legacy unfinished build sites with old ground-level moat-clearance `A` tasks should retire those tasks without clearing valid margin ground/path surfaces, while newer ore-margin backfill tasks still harvest useful exposed resources and immediately restore the surface
- debug and inspection support exists through overlays, build previews, and `/livevillages settlements ...` commands

## Active Workstreams

### 0. Outpost Settlement Integration

Status: In progress

Goal:

- make pillager outposts behave like persistent hostile settlements that players can cautiously join, support, help grow, and raid with, while keeping the MVP pass bounded to settlement membership, standing, construction, recruitment, player raid participation, and readable feedback.
- defer fully autonomous outposts raiding a player's nearby loaded settlement for player defense to post-MVP once the join/support/growth/player-raid loop is stable.

Scope for the current work unit:

- show exact player standing and support progress in debug surfaces, including current rank, support points, and next threshold
- keep loaded outpost population, worker count, and role lists based on raiders plus outpost-member villagers rather than actual villagers only
- award outpost support for useful actions: donating needed materials, completing staged build-site blocks, and supplying martial goods such as arrows/food/weapons; later add defending the outpost from non-member hostile mobs
- recognize exact-item weapons, armor, shields, bows, crossbows, spears, tridents, maces, and practical tools as outpost-valued gear; donated upgrades should be equipped by nearby raiders before leftover items go into stock, while newly recruited outpost members and later replacement equipment draw gear and ranged supplies from settlement stock instead of generating them for free
- keep rank thresholds in one helper so tuning or later config support can change the base values and difficulty scaling without hunting through standing logic
- keep the first-pass rank gates clear: `Tolerated` prevents hostility and sleep blocking, `Associate` makes pillagers stop aiming/staring at the player and exposes better outpost information, `Raider` and above remain hooks for raid-prep and command features
- at `Banner Bearer` or higher, nearby outpost raiders should help defend the player against recent hostile monster attackers inside the outpost influence radius or while the player is traveling near active raid-party members from that outpost
- define `Banner Bearer` as the first raid-participation rank: trusted players receive raid muster notices, can join an outpost raid by traveling with or near the target, and earn support from a successful raid without gaining full outpost command authority
- reserve `Captain` for explicit raid command authority, including triggering raids, denying tribute deals to force an actual raid, and negotiating deals once the interaction design is clear; a goat horn or similar deliberate signal is a candidate trigger
- add first-pass persisted outpost raid state with bounded phases: weekly eligibility, target selection among nearby non-outpost settlements, mustering notices, abstract march timing, control-or-tribute resolution, loot transfer, player support awards, itemized last-raid results, and cooldown
- keep outpost raid phase and cooldown timers on persistent world game time rather than server uptime so reloads preserve the remaining delay
- improve raid consequences: loot should include bounded emerald wealth as well as stock, and raids or tribute should reduce victim security enough to push defensive recovery work
- rotate raid targets among viable nearby settlements instead of always selecting the same village; `Cartographer`, `Roadwright`, `Portmaster`, `Trademaster`, and completed `Lighthouse` capabilities should extend the search radius for raid targets
- make the first visible raid step happen at the outpost by rallying loaded raiders around a banner-carrying pillager during `Muster`, using reachable ground near the tower rather than the tower top
- keep visible raid parties associated with their home outpost while away from the tower, count those active raiders in the outpost census during the march/raid/return phases, keep their first march or return target on a dry land waypoint when the direct target line would pull them into water, treat practical proximity or forward progress past that waypoint as good enough for the next leg, and require enough visible raiders to reach a reachable loaded target-center control point before virtual raid resolution; if combat or attrition drops a visible party below the minimum viable size, trigger return/retreat automatically, while allowing a damaged but still viable party to continue with a reduced arrival quorum; if a visible party cannot reach the target before the maximum march timer, trigger return/retreat rather than leaving it stuck in `MARCHING`
- keep visible raid return behavior after actual raids: surviving participants should navigate back to their home outpost and remain trusted outpost members throughout the trip; return cleanup should only clear the active raid tag from raiders that are actually back near the outpost, and loaded stragglers should keep being steered home instead of becoming hostile orphan raiders
- distinguish Live Villages raid names from vanilla raids: use names such as `Plunder Raid` for ordinary overland outpost raids and reserve `Viking Raid` for later boat-borne variants
- later add a raid bossbar or equivalent HUD timer during the control phase so nearby players can see how long the raiders must hold the village center
- add first-pass `Thralls` as a successful-raid ledger consequence: record them as raid prizes, move population from victim village to outpost, take at most the lesser of half the raider count or `20%` of victim population, prefer unemployed villagers first, skip children, avoid the term `slavery`, and later replace the ledger-only transfer with visible captive return behavior
- use Roadwright-style terrain preference for first-pass raid movement: existing paths/stone roads are preferred for march waypoints, and fuller outpost road planning should later create non-trade raid corridors to nearby villages without making outposts normal trading partners
- expose raid feedback where players already look: outpost nightly reports list itemized raid gains and support rewards, while outpost Trade Boards replace the normal `Routes` tab with a `Raid` tab showing current/last raid status, gains, and player rewards; keep the Raid tab compact enough to show the normal dozen-prize haul without truncating, and keep the `Trade` tab available for donations and barter
- make successful raid loot read as a mixed prize haul when the victim has varied stock: prefer martial valuables and meat/fish foods over vegetable crops, include emerald wealth, and spread transfer across up to about a dozen prize types instead of dumping the whole carry budget into one stock category
- keep debug commands available to clear the nearest outpost raid cooldown and to force an active raid to retreat toward its home outpost during playtesting; retreat steering should use each raider's current position instead of assuming the party already reached the target village
- keep first-pass virtual raid participation generous enough for testing: `Banner Bearer+` players near either the mustering outpost or the target at resolution earn the recorded support reward until visible escort/marching behavior exists
- treat first-pass outpost raids as coercive extraction rather than extermination: success should come from holding the village center/bell/Trade Board area, defeating or outlasting defenders, or accepting tribute; do not require villager kills, do not require all golems to die, and defer chest-ransacking visuals to a later improvement
- add first-pass outpost recruitment/growth: loaded outposts accrue growth without village housing, spend food plus a small amount of valuable stock when recruiting, fill available `Fletcher`, `Forester`, or lower-priority `Roadwright` workstation openings before defaulting back to ordinary `Pillager`s, and keep lower-value civil roles deferred
- expand outpost recruitment from Pillagers to broader Illagers by tier: Vindicators at Tier `2+` capped to half Pillagers, Evokers at Tier `3+` capped to half Vindicators, Illusioners at Tier `4+` capped to half Evokers, and Witches at Tier `3+` only when the outpost has a Witch Hut; outpost-owned hostile members should ignore light-level spawn prevention and should not despawn naturally
- keep outpost construction priorities martial and practical: first-pass abstract projects prefer storage and defenses over civic village projects; later expand concrete structure priority toward `Trading Post`, `Fletcher Hut`, and `Forester Workshop`
- keep placed workstation previews independent from outpost priority, so a low-priority Cartographer's House or other valid structure still shows a wireframe and can be player-built even if the outpost would not choose it next
- count outpost housing pressure only against non-raider members, since pillagers and guard raiders do not use village beds; loaded outposts should still reconcile actual beds for villagers and other bed-using members
- keep loaded outposts eligible for nightly report files, including an observed member census even when most members are raiders rather than villagers
- keep outposts on the same base `96` block settlement footprint as villages/custom settlements for maps, workstation linking, palisades, and edge infrastructure, while preserving smaller local checks for active work such as nearby fallback construction
- keep only Live Villages-spawned outpost recruits persistent, count vanilla raiders only while present, trim excess unmanaged raiders when a loaded outpost has already ballooned past its cap, and require a nearby outpost member for loaded fallback construction block placement so construction progress does not appear to teleport across the outpost
- prevent a settlement from starting more than one `Trading Post` build site; additional Trade Boards may link to the settlement, but should not create duplicate trade-post projects
- keep loaded-world materialization for virtual improvements, especially Trading Posts completed by settlements before the player first loads the area; first pass places a linked Trade Board near the bell when possible and starts the normal staged Trading Post build for populated loaded settlements that lack both
- keep outpost villagers protected and counted as outpost members; defer pillager-style profession uniforms until the behavior is stable
- defer full player-led raid command controls until the first-pass persisted raid foundation is stable enough to trust target selection, tribute, loot transfer, participation credit, and cooldowns across reloads

### 1. Economy, Harbors, And Trade

Status: In progress

Focus:

- keep abstract and loaded settlement outputs aligned, especially for harbor-adjacent production such as `Fisherman`
- keep loaded `Fisherman` boat trips bounded to dock-adjacent water and back so they stay visible without stranding villagers past the evening gathering
- make infrastructure bonuses visible in actual trade cadence, quality, and route usefulness, not only in hidden scoring
- surface active `Trading Post` and other build-site progress directly in the board/overlay project views so route/build/trade state is legible before retuning deeper priorities
- keep Trade Board goods panes usable with longer inventories by scrolling instead of truncating rows, and keep row selection visuals aligned with the actual click targets
- keep Trade Board inventory rows aggregated by exact item type so duplicate stacks do not crowd the list, while still letting village stock retain the abstract recognized-goods model where that supports settlement simulation
- keep Trade Board player-row refreshes authoritative after donations and trades so aggregated exact-item rows update counts immediately and do not break when the first contributing stack is exhausted
- keep Trade Board transaction refreshes recomputing build-site material demand immediately so `Give 1`, `Bundle`, `All`, and village-side trades always leave `Need` counts and stored stock in sync
- keep shared settlement trade valuation recipe-manager-aware and cached, so produced goods from vanilla, data-pack, modded, baker, carpenter, fletcher, mason, smelting/refining, and later workstation paths do not sell below known input cost even when surplus pricing discounts them, without doing recursive recipe scans during player interaction or route ticks; reserve hardcoded fallbacks for settlement-only abstractions that have no recipe data
- keep miner-facing support and extraction goods legible in the Trade Board: `dirt`, `ladder`, `raw iron`, and `raw copper` should be known early, while `redstone` and `diamond` unlock as known board goods at settlement `Tier 2`
- keep non-empty carried containers such as `Bundle` separate from aggregated rows, preview their contents on hover, and donate their contents into settlement stock without trading away the still-useful empty container
- keep Trade Board shortage/surplus output accurate when construction blockers, protected reserves, or build-site demand are involved
- keep automatic ore / glass refining fuel-aware so the village spends real `coal` / `charcoal` / wood fuel, prefers efficient fuel sources, and preserves protected wood reserves when possible
- keep route-transfer demand aware of active staged build-site materials so visible construction stalls can be supplied by trade
- preserve completed harbor/trade build-site effects for route planning even while the corresponding chunks are unloaded
- keep route growth limited by reachability and active planning state rather than a small hard cap on total connections
- keep Roadwright land-route planning dry and harbor-aware so survey/roadwork intent stops at shore access instead of extending explicit improvements over water
- keep dock and lighthouse build sites out of ordinary Roadwright door/body targeting; docks should contribute only dry land-side Portmaster-anchor access and lighthouses should contribute only dry base access without falling back to water-facing structure endpoints
- push Roadwrights toward external settlement corridors and `Mileposts` once core local path connectors are already in place, instead of overbuilding low-value internal spurs
- let trade demand support limited brokered wants for downstream partner shortages so multi-hop trade networks can emerge
- unblock recipe/data-driven content so new recipes are dependable before more professions are added
- keep route trade capacity value-aware, so costly produced goods consume more route throughput without becoming impossible to move on reasonable routes
- keep emerald accumulation and trade incentives compatible with future settlement-tier unlock thresholds

### 2. Settlement Advancement And Tiers

Status: Planned

Focus:

- add persistent settlement-tier state instead of a volatile wealth-only check, with `Scribe` support preventing regression after a tier has already been reached
- use combined emerald-wealth and population gates for early tiers; first-pass targets are `500` / `16` for tier `2`, `5,000` / `32` for tier `3`, and a provisional `20,000` / `48` for tier `4`
- make tier changes visible in the `Trade Board`, overlays, and debug output
- gate structure selection, upgrade eligibility, path materials, and fortification projects on settlement tier
- keep tier rules data-friendly so thresholds and per-structure unlocks can move out of Java later

### 3. Defense And Worker Behavior

Status: In progress

Focus:

- bell response should separate noncombatant panic from rally behavior for combat-capable professions
- bell influence radius should be `max(25, half settlement radius)` rather than a tiny fixed center
- keep profession priority sane so urgent defense interrupts the right workers without making routine work feel erratic
- keep villager defense contributions legible in loaded combat with lightweight attack sounds or particles, rather than invisible help that players cannot verify
- let loaded `Fletcher`s use completed palisade slab walks as occasional patrol/firing posts, shoot visible hostiles from those safer positions, and retreat toward work/stock access for a short time after recent injury
- count completed palisade gatehouses and proven wall columns in the settlement security calculation so visible wall coverage raises safety instead of only physical path blocking
- keep evening behavior coherent: miners should surface in time for the social gathering, home-bed assignment should settle into quick repeatable nightly returns instead of fresh bed scrambles, and throttled return diagnostics should expose path/no-home/hostile-visibility causes when villagers fail to arrive
- keep bed-linked profession staffing driven by real built beds in the linked structure, so structures such as the `Bakery`, `Trading Post`, `Roadwright's Workshop`, and `Fletcher Hut` open worker slots only as their own beds are actually built; `Carpenter` and `Forester` are narrow Tier 1 bootstrap exceptions and may each use one visible workstation/build-site slot before the workshop bed is complete so they can supply construction wood; loaded Tier 1 support bootstrap ranks `Forester`, `Carpenter`, and `Miner` workstations from active stock/build-site shortages, places at most one new support site per discovery pass, suppresses player-facing blocked-placement signs, and spaces candidates away from existing support workstations and build-site footprints
- include Butchers in the shared profession-demand recruitment pass when a settlement has reachable smoker job sites, so meat shortages can produce actual butcher workers instead of only appearing as unresolved inventory needs
- keep actual-villager census roles specific for Butchers and Fishermen rather than folding every vanilla food profession into Farmer, so stock production, reports, and debug population agree
- consume staple foods before scarce reserve foods such as cod and meat, so profession-produced foods can build visible settlement stock instead of being immediately eaten while crop stores remain abundant
- keep villager home ownership distinct from the actual nightly bed target, so a villager can keep a preferred assigned home bed yet temporarily sleep in another reachable bed when pathing to the preferred one fails; rest-time home maintenance now actively nudges non-sleeping villagers toward the assigned sleep target and starts bed sleep directly once they are already beside an unoccupied bed
- keep loaded profession work from overriding evening gathering or rest navigation; job loops should yield once the gathering window starts unless they are explicitly returning a worker, such as a Miner surfacing or a Fisherman docking
- keep daily profession progress reports scoped to loaded settlements for now and gated by the default-off `daily_settlement_reports` config flag and default-off `live-villages:daily_settlement_reports` game rule; when enabled they should be written as end-of-day files in `livevillages_exports`, resilient to player sleep skipping the late-night write window, with a section for every vanilla and Live Villages profession even when nobody is assigned, plus each loaded villager's actual completed mined/harvested blocks, produced goods, mined drops, recovered goods, consumed work inputs, status/death notes, accomplished work, and inter-settlement trade batches; do not include observed intent states or abstract simulation stock credits as villager work
- keep throttled profession diagnostics available during playtests so Carpenter, Baker, Forester, Fisherman, Fletcher, Mason, Butcher, route trade, and workforce recruitment report why loaded workers are idle, missing inputs, blocked by schedule, missing workstations, or still moving toward work
- keep Miner diagnostics equally explicit during playtests: log no mine entrance, no matching mine site, no selected task, moving to target, failed task execution, mine-escape/rescue movement, and daily-cap states with shaft, ladder, stock, target, and reachability details
- miner evening return should use bounded multi-step ladder/tunnel movement, exact ladder-position preference, side-tunnel routing back to an existing ladder, rescue ladders for bedrock-level shaft traps, both top ladder columns as valid exit columns, near-mouth exit tolerance for the final few ladder rungs, broad rough-lip surface exit search, fall-distance clearing on scripted rescue movement, and throttled surface-return diagnostics so deep miners can surface within the gathering/rest window instead of stalling one block above a ladder segment or stranded in a branch, including a bounded rescue snap back to a nearby known mine ladder when path search cannot produce a step
- trapped non-miner villagers near a loaded mine should use the same bounded shaft/escape rescue movement instead of repeatedly trying to path back into the place where they were trapped, including when they are stuck at the top ladder mouth; Foresters are explicitly surface-only and should drop any forestry task when the mine-mouth avoidance rescue moves them out of a loaded shaft column
- mine-mouth rescue must treat both ladder columns and open shaft-mouth columns as hazards; a surface worker already near the top of a loaded mine should be snapped to a standable surface exit before ordinary pathing or profession work resumes
- underground Miner movement should use bounded scripted mine-network steps between ladder rungs and selected tunnel/ore stand positions instead of depending on vanilla pathing inside the vertical shaft; this prevents workers from sitting on a bottom rung or falling below the completed ladder band before the task can execute
- keep loaded Miner assignments sticky between resource-maintenance passes so movement toward a chosen shaft/tunnel/ore target continues every tick, work reach uses block-to-block distance consistent with task selection, surfaced miners use scripted overworld steps toward gathering/home when vanilla pathfinding cannot route out of the mine mouth, adjacent exposed ore is mined as a vein before distant targets are chosen, shaft ladder support should be derived from the intended two-ladder/two-open-column shaft layout instead of damaged local terrain, starter shaft repair should restore missing solid shell/support blocks and clear blocked open columns before optional mining or lighting, recently failed or stale mining targets are skipped briefly instead of being reselected into ladder/tunnel loops, hostile shaft or tunnel cavern breaches place warning signs and suspend shaft/cavern work until the player clears the threat and removes the sign, and miner work throughput stays high enough for playtesting through more actions per maintenance pass and faster decide cadence
- keep forester planting conservative near settlement structures and existing trees: new saplings should stay at least a few blocks clear of structure footprints, nearby saplings, and mature tree bases so forestry reads as managed spacing rather than crowding
- treat underground worker settlement membership as a horizontal-radius cylinder for census and home-assignment purposes so deep miners still count and keep their village behavior
- extend loaded Miner work beyond the starter shaft by cutting upper-level-first `2`-wide by `3`-tall primary side tunnels to the settlement radius as eligible shaft levels are reached, not only after the shaft hits bedrock, about every `5` levels with `2` solid levels between them; once a level's primary tunnels are full length, add perpendicular `1`-wide by `2`-tall secondary tunnels to the settlement radius with wall torches every `9` blocks, temporary `dirt` / `cobblestone` floor supports when needed, preferring dirt before cobblestone, exposed-face ore scanning so visible resources are not left untouched, reachable-resource scans from existing mine ladders for exposed copper, redstone, and geode resources, discovery signs for notable features, monster-closure signs that block only the unsafe primary branch, and reachable shaft-wall stone/ore fallback mining when no branch task is available
- keep cave-breach mining bounded and reachable: skip cave lighting or ore tasks whose stand position cannot return to a known mine ladder or is below the completed ladder-bottom safety band, so unreachable/deep cave pockets do not monopolize the miner task loop
- keep evening gathering anchored sensibly for bell-less settlements: prefer the `Trade Board` / `Trading Post`, then another central non-home POI, instead of dropping straight to an arbitrary center point, and prefer a standable nearby access tile over the raw POI block when choosing the loaded gathering target
- preserve readable overlay and debug output for current worker tasks while this logic evolves

### 4. Construction And Profession Structures

Status: In progress

Focus:

- continue turning staged construction into generic reusable infrastructure instead of a set of one-off structure rules
- add generic duplicate suppression, repair reopening, and upgrade handling across all staged structure types
- make structure blueprints and retrofit logic tier-aware without requiring every tier bump to place a duplicate building
- keep hostile outpost construction visibly slower than village specialist construction: loaded raiders may advance staged build sites by scavenging/extorting one block at a time, while richer outpost recruitment, profession weighting, pillager-style civilian skins, and player-led raids remain later work
- keep the first-pass authoring capture tool bounded and text-export oriented: capture the looked-at structure, normalize facing/layers, write a file for later curation, tolerate tall vanilla structures such as pillager outpost towers, and avoid turning the first implementation into a full editor
- make the first-pass `Tier 1` palisade concrete: log wall at least `4` blocks high, interior slab firing walk below the top log with neighboring slabs differing by no more than a half block where possible, torch spacing about every `10` blocks, stair access about every `30` blocks, and simple path-aligned gatehouses
- keep early fortification radius smaller for starter villages, with `Tier 1` aiming for about `80%` of settlement radius before later tiers expand toward the full radius
- make placement-block recipe credit site-bound instead of settlement-stock-bound for `Housing Shelter`, `Simple Housing Shelter`, `Lighthouse`, and `Palisade Gatehouse`; other workstation blocks such as `Baker's Counter` should keep ordinary start/resume behavior without implicit recipe-credit
- replace committed `Lighthouse` markers with cobblestone and committed shelter markers with their planned door; add `Palisade Gatehouse` markers that replace themselves with the door ingredient once the gatehouse build is committed
- add first-pass `Palisade Gatehouse` and `Palisade Point` support: iron-bar and copper-bar gatehouse variants, eight-way placement/preview including diagonals, radius-percent preview feedback, staged gatehouse blueprint, craftable wall-control points, and wall segments between neighboring controls near the intended `80%` radius
- keep `Palisade Point` wall planning dynamic rather than blueprint-fixed: when a point is placed, connect it to the nearest palisade controls by angle around the settlement center, and if an existing wall extension already reaches out from either neighbor, continue from the extension end closest to the new point
- when a `Palisade Point` is placed between two neighboring controls, retire the incomplete direct wall segment between those controls before adding the new point-routed segments so workers do not clear the marker as an obstruction from the old route
- also retire incomplete palisade wall sites whose planned log path passes near the new `Palisade Point`, and treat the point marker as a protected wall-control block rather than generic disposable construction clearance
- include the `Palisade Point` column in the generated wall site and treat nearby player-built vertical log columns as valid local palisade reroutes so workers do not force small manual corrections back to the original preview line
- suppress and retire duplicate palisade wall build sites when the proposed route is already substantially covered by completed palisade build sites or observed four-high log wall columns, including overlapping-settlement edge cases where the physical wall exists even if the active build site belongs to another nearby settlement
- reject and retire stale palisade wall build sites when their planned route crosses water or has large discontinuous gaps from skipped invalid columns, so previews and workers do not preserve detached wall fragments near rivers or ponds
- normalize existing palisade edge cases during loaded-world maintenance and preview: fix legacy gatehouse marker columns into door targets, pull any stale palisade slab-walk positions back down from the old doubled-Y calculation, and add missing Palisade Point endpoint log columns to older wall sites
- keep first-pass `Palisade Gatehouse` anchoring wall-like instead of house-like: the placed marker/lower door is the ground-level blueprint origin, the interior top-half slab firing walk faces the settlement center, torches/exterior details face away from the settlement center, the marker is replaced by the planned door, and oxidized/waxed copper bars still count as valid copper bars
- make later palisade wall routing structure-aware: if the course intersects a perimeter structure, use the outward side of that structure as part of the wall line where possible, connect palisade columns to that face, and add a top log course over low roofs when needed for continuity
- after a build site is complete or substantially present in the world, treat its old air-clearance and margin-cleanup positions as permissive player-improvement space; repairs should reopen only for required structure blocks, not to remove later stairs, access paths, palisade extensions, lights, or other adjacent improvements
- keep preview and site-validation logic permissive for removable natural vegetation so trees or brush do not falsely block otherwise valid builds
- keep workstation placement and held-item previews valid inside known outposts, while preserving the broader near-settlement fallback for non-outpost villages
- keep structure-spacing validation strict against existing crafted infrastructure while allowing natural terrain or vegetation to be cleared, with special handling so simple shovel-made dirt paths do not block placement and improved route surfaces only block the actual footprint
- keep footprint fill validation consistent with the rest of placement rules so clearable natural blocks inside raised or filled columns do not falsely fail shifted previews
- treat exposed natural ore and buried natural roots or branches as ordinary terrain support for preview validation when they do not require moat clearing, so forests and rocky ground do not create false negatives
- resolve ground columns by drilling through removable canopy clutter and the empty air directly beneath it before judging stability, so overhanging branches do not anchor false “ground not stable” failures
- keep the moat floor intact during ordinary structure-margin clearing; only recover explicit ground-level resource blocks there, and immediately backfill those spots with dirt or matching route surface
- surface prospective placement failures directly in the build preview with a short reason string and highlighted blocker positions so invalid sites can be diagnosed in-world
- align road-quality detection and Surveyor map rendering with the intended upgrade ladder: `Dirt Path` -> `Cobblestone` -> `Smooth Stone` -> `Stone Bricks` / `Bricks`, while ignoring plain natural `Stone` and `Gravel` as autonomous road upgrades
- let build previews distinguish exact block matches from compatible wrong-material matches, and let compatible player materials complete structures without blocking functionality
- keep loaded construction reconciliation aligned with that same compatible-material rule: biome-preferred palettes should influence the planned target and later upgrade desire, but compatible player-used wood or starter-stone variants should not be auto-rewritten during ordinary completion just because hinge, stair-shape, gate-wall, or similar secondary state differs; assisted placement should keep the player's compatible material block, copying only the planned placement properties, and worker placement should use compatible local material states for generic construction stock rather than forcing an older planned species when the form matches
- keep the Miner rollout split between the now-staged `Mine Entrance` build, the loaded shaft-deepen / exposed-vein / shaft-lighting / cave-breach / bedrock primary-and-secondary-tunnel / stone-fallback loop with visible shaft descent and end-of-day ascent, the new fuel-aware raw-ore refining support for `iron` / `copper`, and later quarry and richer refining behavior
- keep profession-linked housing sticky where structures provide beds, while leaving Miners dependent on ordinary settlement housing rather than inventing beds in the `Mine Entrance`
- treat bed-bearing profession structures as the first home-bed source for villagers assigned to that workstation, so workforce capacity and home assignment stay coupled instead of competing with general settlement beds
- keep Mine Entrance preview readable for excavation-heavy footprints; planned shaft voids should be visible in the wireframe instead of disappearing, the side access recess beside the ladder should stay only one block deep, and the site rules should require a clear front entry approach without forcing the whole hillside facade to be carved back
- improve physical stock pickup and deposit behavior so visible worker logistics match settlement accounting more closely
- keep profession production accounting visible during playtests: daily file reports should attribute real meat, leather, wool, cod from visible fishing, exact harvested logs, exact mined block ids, mined/recovered goods, bakery outputs, route trade batches, villager deaths, and Carpenter/Mason/Fletcher/Farmer conversions or harvests to loaded villagers where the worker loop knows the villager, including consumed work inputs.
- replace overly magical auto-crafting assumptions over time with clearer workstation-linked supply flow
- keep biome-aware structure palettes in sync across previews, staged builds, and contributor docs
- keep settlement naming biome-aware too, so local names fit the terrain and hostile sites read as threatening instead of pastoral
- add a later blueprint-import workflow that can take an exported blueprint file, let the player pick it through a file-selection dialog, package it into a generic named construction-anchor block, and start staged villager-assisted construction from that imported plan
- add a later `Tier 3+` luxury-upgrade pass where a few profession structures can prefer imported foreign-biome style variants to create prestige-material trade demand
- keep blueprint symbols explicit for contributors; reserve shorthand aliases like `C` for backward compatibility only
- keep directional blueprint row order standardized for contributors: first row is the rear, last row is the front unless an inline comment documents an intentional exception
- keep a contributor-friendly blueprint authoring guide so non-technical builders can supply layered structure designs without reading Java internals
- the first-pass `Mason` workshop and loaded masonry-support loop now exist; `Mason` now prefers stone-heavy staged construction blocks while `Carpenter` prefers wood-heavy ones, and idle `Mason`s can top up `Tier 1` cobblestone stock at the `Stonecutter`; keep follow-up work focused on richer fortification labor, broader stone-stock processing, and later-tier material-upgrade passes
- `Mason` now also cuts finished `milepost` goods at the `Stonecutter` from surplus stone stock, while `Roadwright` consumes that stock to place real roadside markers beside established external routes instead of conjuring markers directly
- first-pass loaded `Milepost` placement now keeps markers outside settlement bounds, prefers settlement-edge entry corridors and obvious established-road junctions, aims for roughly `100`-block spacing along land routes, skips duplicate placements when another `Milepost` is already within about `50` blocks of the desired spot, and lets outward roadwork resume from the farthest already-established route anchor instead of restarting every external push from the village core
- the `Surveyor's Table` road overlay now scans loaded path surfaces more faithfully instead of dropping most road columns or missing gravel / shallow-buried road blocks, and loaded `Roadwright` planning now fans out from the settlement core and best existing route anchors so local POIs, building entrances, mileposts, and nearby land-route settlements tighten into one visible network more reliably
- the fog-off `Surveyor's Table` map now needs to behave like a full loaded-world survey rather than a sparse debug sample: road/path caps should be high enough for larger settlements, authored build footprints and obvious world-built structures such as palisades should render as building/project coverage, and natural `stone` cliffs should not be mistaken for improved road surfaces
- survey fortification detection should stay strict enough to show adjacent log-and-slab palisades without turning ordinary tree trunks into fake building dots, and the map should also expose shoreline or harbor water so docks and coastal defenses read clearly
- `Trade Board` founding previews now work outside existing settlements: a board inside a settlement radius joins that settlement, a board outside all settlement radii can found a new `Tier 1` settlement when clear, and placement should reject too-close founding attempts with a visible message instead of silently linking across open terrain
- natural flowers and similar loose vegetation should stay clearable during structure placement, while bordered garden beds, potted flowers, and other obviously decorative planted structures still count as blockers
- when a recognized workstation is already inside an existing sheltered structure, accept that building as the workstation's completed structure variant instead of placing a `can't build here` sign or forcing a duplicate staged build nearby

### 5. Roads, Maps, And Route Planning

Status: In progress

Focus:

- keep `Roadwright` responsible for both internal route quality and outward settlement connections
- keep route vocabulary explicit: routes are traversable links between POIs, while paths or roads are the visible terrain improvements laid along those routes
- keep the `Surveyor's Table` map sourced from current loaded-world truth so visible roads and improvements do not lag behind villager survey chores
- keep custom `Trade Board`-founded settlements and outposts on the same effective footprint as regular villages so shoreline anchors, lighthouses, palisades, Mine Entrances, and other edge infrastructure are not silently excluded by a smaller radius
- keep shoreline harbor structures using land-side access targets only; roadwright path planning should not treat air above water as a valid standable route endpoint
- keep completed Portmaster docks attached to their anchor for future checks, so old anchors do not re-advertise invalid dock previews after the water surface has been replaced by finished planks
- keep active build-site and held-workstation previews ahead of generic placed-infrastructure previews, and only show invalid dock-placement messages when the player is actually targeting that anchor/preview
- extend route quality improvements without regressing the bounded-search and bounded-per-tick performance model
- add bounded catch-up for visible road/path improvement so revisiting a settlement shows progress without requiring constant player babysitting
- keep route planning biased toward existing path surfaces and nearby `Mileposts`, and keep first-pass route targets centered on bells, structure entrances, standalone workstations, civic anchors, and neighboring settlements
- make internal path materials and road-furniture upgrades respect settlement tier and unlocked civic quality
- keep `Surveyor's Table`, harbor map views, and `Milepost` guidance useful for both debugging and gameplay

### 6. Profession Rollout

Status: Planned

Next in line:

- finish broader loaded-world `Butcher` polish where current first-pass behavior is still coarse
- preserve the current harbor-focused `Portmaster` and boat-assisted `Fisherman` pass before adding new maritime scope
- continue `Fletcher` and `Carpenter's Bench` efficiency follow-ups where workstation gameplay still needs tuning
- add remaining missing role loops only after their settlement/economy hooks are clear

## Ordered Backlog

Use this as the current sequencing guide, not a promise of exact release order:

1. Harbor trade validation and recipe-path fix.
2. Settlement tier state, unlock rules, and UI exposure.
3. Bell/rally defense split.
4. Generic staged-construction repair, duplicate suppression, and upgrade helpers.
5. Better visible stock logistics for workers.
6. Roadwright and map-quality follow-up.
7. Butcher loaded-world polish.
8. Additional profession rollout.
9. Later civic/designation blocks and outpost behavior.

## Durable Decisions

Keep these unless the spec is intentionally changed:

- `Carpenter` is the custom wood-focused construction profession.
- `Mason` is shared stone/construction labor, not the primary wood builder.
- `Pathwright` is merged into `Roadwright`.
- `Surveyor's Table` is the `Roadwright` workstation.
- `Milepost` is route infrastructure, not a workstation.
- `Cartographer` gates efficient long-range land route planning and trade beyond about `32` chunks / `512` blocks in first-pass route planning; `Scribe` support can stack extra range and quality after that cartographic gate is met.
- The `Trade Board` anchors a staged `Trading Post`, with a future `Shopping Mall` upgrade on the same site.
- Settlement advancement uses four civic tiers. Settlements should rise immediately when they meet a new tier gate, but regression protection should come from `Scribe` support rather than from a universally sticky never-downgrade rule.
- `Scribe` is an advanced Librarian-style role: keep baseline book/bookshelf production on ordinary `Librarians`, and put recipe trading, settlement-to-settlement knowledge exchange, route intelligence, and civic-memory support on `Scribes`.
- First-pass Scribe foundations are implemented: `Scribe Desk` block/item/recipe/assets/POI/profession registration, staged `Scribe Office` anchoring and previews with a role-specific storage-heavy office blueprint, bed-linked Scribe recruitment with Librarians preferred as advanced candidates, Scribe census support for existing trade-route bonuses, persistent settlement-known starter recipe storage, a selectable Scribe Desk recipe-trading UI that teaches unknown settlement-known recipes for tiered paper/book payments, lets players contribute player-known non-special recipes into the settlement ledger for modest support rewards, shows those rewards in contribution tooltips, uses display-derived recipe result icons when available, removes successful recipe rows from the open client and server menu lists, rejects stale recipe-row clicks by recipe fingerprint, revalidates that the desk block and settlement link are still current before server-side learn/contribute actions mutate state, and capped settlement-to-settlement recipe exchange during route trade cycles when Scribe support is present.
- Remaining Scribe MVP work: tune route-exchange limits, contribution reward values, and pricing further after playtests.
- First-pass Guard foundations are implemented: `Guard Post` block/item/recipe/assets/POI/profession registration with a shared `5`-guard job-site capacity, staged `Guard Post` anchoring and previews with a role-specific fenced-apron blueprint, bed-linked Guard recruitment and preferred home assignment, actual Guard census support without double-counting the older abstract fallback, Guard participation in loaded bell defense, direct Guard Post donations for swords, armor, shields, ranged weapons, ammunition, and banner trophies, stock-consuming Guard equipment upgrades for swords, armor, and shields during loaded bell defense, first-pass ranged Guard shots that claim donated bows/crossbows and consume arrows against line-of-sight bell-response targets, first-pass escorts for nearby loaded workers carrying construction supplies or collected goods or performing higher-risk forestry, fishing, harbor, or mining work with simple task priority, one-Guard-per-target coverage, and a route-corridor scoring bias when forecast Roadwright route traces are available, simple loaded Guard Post patrols when no bell alarm or escort is active, capped security/comfort effects from stored `Desecrated Enemy Banner` trophies, and optional exterior trophy banner placement on completed Guard Posts that avoids doors, roads, storage, beds, ladders, gates, and workstations.
- Remaining Guard MVP work: refine broader route-security policy and settlement priorities beyond the first-pass loaded escort scoring bias; also consider a custom banner-pattern asset for a cleaner red no-symbol overlay if vanilla banner patterns are not visually strong enough.
- First-pass Gardener foundations are implemented: `Gardener Workstation` block/item/recipe/assets/POI/profession registration, staged bedded `Gardener's Shed` anchoring and previews with a role-specific fenced-yard blueprint, bed-linked Gardener recruitment and preferred home assignment, actual Gardener census/report support, Guard/Scribe/Gardener report role coverage, Gardener comfort contribution, unloaded egg and flower output without duplicating Farmer staple crop output, craftable `Sling` presentation, weak bell-response sling shots, a pass-local per-workstation task-plan cache for shared loaded chicken, pen, and flower-bed decisions, a loaded workstation loop that feeds nearby real chickens from wheat seed stock before producing egg stock, a bone-meal-to-seed fallback, capped raised dirt-and-flower placement near the workstation that biases toward paths, exterior walls, under-window and beside-window placements, nearby doors without blocking them, and places of interest without using road/path surfaces or immediately blocking doors, optional trapdoor edging for raised beds from settlement stock, and a first-pass chicken pen build/herding loop that reuses partial nearby pens and scores new pen sites toward chickens and village-visible areas.
- Remaining Gardener MVP work: tune whether Gardeners should seek cover or retreat after sling shots during playtests.
- First-pass Beekeeper foundations are implemented: `Honey Separator` block/item/recipe/assets/POI/profession registration, staged bedded `Beekeeper's Apiary` anchoring and previews with a role-specific fenced-apiary blueprint, bed-linked Beekeeper recruitment and preferred home assignment, actual Beekeeper census/report support, honey goods and generic flower stock in settlement stock and Trade Board goods, unloaded honey production, comfort contribution, a pass-local per-separator task-plan cache for shared loaded smoke, harvest, hive-placement, flower-placement, and bee-breeding decisions, a loaded honey bottle/honeycomb/candle/bee-hive stock loop, bounded loaded placement of up to `3` hive-over-campfire setups and capped flowers near each separator from settlement stock with first-pass hive/flower/open-air placement scoring, hive-capacity-aware feeding of nearby breedable adult bees from flower stock that counts visible bees plus bees inside nearby hive block entities, first-pass inspection/harvest of smoked full bee hives and bee nests, managed hive smoke relight/replacement, direct Honey Separator glass-bottle-for-honey-bottle trades against settlement stock, and a mostly white protective-suit profession texture for villager and zombie-villager variants.
- Remaining Beekeeper MVP work: tune the protective-suit texture after in-game review if it needs stronger veil or glove readability, and improve long-term apiary ecology beyond first-pass hive harvesting.
- `Tier 3+` may unlock selective imported style preferences for a few structures, but those should behave like luxury upgrades rather than forced global palette swaps.
- Recognized workstations and structure anchors should start staged builds when possible; do not fall back to instant full structure placement.
- Loaded construction supply pickup is opportunistic: workers should use a nearby, real, standable, reachable `Trade Board` / `Trading Post` access tile when one exists, but a missing, distant, or unreachable stock point must not stall block placement from abstract settlement stock.
- Craftable structure-anchor blocks are the preferred trigger for non-workstation civic structures unless a vanilla workstation is the better anchor.
- The `Trade Board` should show highest-priority settlement-wide wants; profession structures should handle narrower specialist trades.
- First-pass profession-structure trade outlets now include direct `Honey Separator` glass-bottle-for-honey-bottle trades, settlement-linked `Brewing Stand` potion-ingredient-bundle-for-healing-potion trades, settlement-linked `Lectern` paper/leather-for-book and book/planks-for-bookshelf trades, settlement-linked `Smoker` wheat-for-stocked-beef/pork/leather trades, settlement-linked `Cauldron` leather-for-stocked-leather-armor trades, settlement-linked `Loom` wheat-for-wool and wool/planks-for-bed trades, settlement-linked `Fletching Table` arrow-ingredient-bundle-for-stocked-arrow trades, settlement-linked `Blast Furnace` iron/plank-for-armor-or-shield trades, settlement-linked `Smithing Table` iron/stick-for-pickaxe trades, settlement-linked `Grindstone` iron/stick-for-sword trades, settlement-linked `Stonecutter` rough-stone-family-blocks-for-stocked-`Milepost` trades, and settlement-linked `Carpenter Bench` log/plank-for-stocked-woodwork trades. Vanilla workstations outside a settlement pass through to vanilla behavior instead of claiming the click.
- Trade Board actions use an explicit client-to-server action payload carrying the rendered row key and goods keys rather than large encoded menu button IDs, so stale or shifted rows refresh safely instead of executing a different visible trade or donation.
- Trade Board goods coverage includes `cod` and `Desecrated Enemy Banner`; server-created banner payouts use the same ominous-banner base plus red circle and slash overlay used for Guard Post trophy displays.
- Custom settlement screens take a tier-specific theme snapshot when opened, and bundled screen colors now live in `assets/live-villages/screen_themes.json` so higher-tier palettes can be edited without changing Java constants.
- Worker task labels now cover Beekeeper loaded hive/flower/smoke/harvest tasks and vanilla production tasks such as brewing healing potions, copying books, assembling bookshelves, collecting wool, weaving beds, smithing iron tools/weapons, and forging armor.
- Trade Board trading stays centered on `Trade` and `Bounties`: `Trade` handles player-selected exchange and direct donation, while `Bounties` exposes the settlement's highest-priority wants without pretending that wants are a second full trade list.
- The Trade Board goods panes are a mixed widget surface: keep `ObjectSelectionList` for the left-side `Trade` inventory list, while `Bounties`, map screens, and recipe grids stay custom because they are not a good one-column list fit.
- Non-empty carried containers should use a dedicated `Donate contents` path instead of the normal donation buttons, and their contents should move into village stock rather than storing the full container as opaque stock.
- Inter-settlement trade should eventually support direct `emerald` movement and a small amount of brokered demand for goods wanted by downstream partner settlements.
- Keep refining how arbitrary donated inventory goods are priced, grouped, and explained once they enter the village-side trade inventory, but keep settlement wants visible through the dedicated `Bounties` tab even when the player currently lacks matching goods.
- Player-placed vanilla `Cartography Table` blocks should anchor a staged `Cartographer's House`.
- Player-placed vanilla `Smoker` blocks should anchor a staged `Butcher Shop`.
- The vanilla `Fletching Table` should remain a real player-use arrow workstation with material-arrow variants rather than staying decorative.
- First-pass loaded `Fletcher` work now produces `Copperhead Arrow` stock from stick, feather, and copper arrowhead material when available, falls back to vanilla arrow batches from flint, and recognizes Copperhead/Ironhead/Diamondhead arrows plus copper/iron nuggets as settlement trade goods; premium-arrow autonomous production and combat selection remain follow-up balance work.
- Butcher and Shepherd first-pass pen planning should build independent new pens rather than silently attaching extra fence lines to already-detected pastures.
- Contributor onboarding docs should keep a beginner-friendly Windows path available: VS Code, GitHub, Temurin JDK, and repository clones under the user's home `Projects/` folder by default.
- Contributor docs should also keep GitHub bug reporting easy for young playtesters, visual contributors, and coders, with a short plain-language path instead of assuming heavy issue-template discipline.
- Contributor docs should define a plain-language glossary, a generic playtest guide, and a clear asset-layout guide, and source-art files such as Blockbench projects should live outside shipped runtime resources.
- Contributor docs should also explain, in plain language, how commits, syncing with `origin/main`, and pull requests fit together so contributors do not mistake a local commit for a project-submitted change.
- `AGENTS.md` should reinforce those contributor Git/GitHub best practices so AI-assisted contributors get branch-state checks, stale-branch rescue guidance, logical commits, and pull-request reminders by default instead of only when they remember to ask.

## Performance Guardrails

These matter enough to keep in the plan:

- Prefer cached scans, remembered intent, and bounded work queues over repeated broad rescans.
- Broad loaded-world analysis must have an invalidation or gradual refresh strategy.
- Avoid full-area scans inside hot tick paths unless the work is explicitly throttled and measured.
- When adding new loaded simulation, favor local surveys plus saved state over rebuilding the whole settlement picture each tick.
- If a feature risks becoming expensive, add timing logs or similarly measurable instrumentation early.

## Later Scope

Keep these visible, but not as active work:

- richer `Beekeeper` hive/campfire/bee-breeding/apiary-placement behavior, later Miner behavior beyond the new first-pass shaft-deepening, ladder descent, exposed-vein, shaft-lighting loop, primary and secondary side tunnels, and first-pass fuel-aware `iron` / `copper` refining, including quarry cuts, richer mined-resource stock handling, broader ore refining, and tunnel-lighting preferences, richer `Gardener` raised-bed/flower/chicken behavior, richer `Guard` trophy/ranged/patrol behavior, fuller `Leatherworker`, `Shepherd`, `Cleric`, `Armorer`, `Toolsmith`, and `Weaponsmith` equipment/combat-presentation behavior, `Scribe` playtest tuning, and fuller warehouse behavior
- later civic designation blocks such as apartment or keep anchors
- expanded street-light, bridge, and broader civic-improvement behavior
- a generalized blueprint-import feature that may be worth extracting into a standalone sharing/build-assist mod once the import, packaging, and staged-construction UX are mature
- hostile outposts as self-building raiding settlements with different staffing and growth rules beyond the current parley / `Tolerated` / `Associate` trust foundation
- boat-borne coastal or river outpost raid variants that use completed docks or later port infrastructure, after water routes and boat behavior are mature
- mounted pillager patrols and raiding parties once outposts can acquire, tame, train, and maintain horses or other mounts

## Maintenance Notes

- Prune completed bullets aggressively once they are no longer decision-relevant.
- Do not turn this file back into a chronological shipped-feature ledger.
- If a completed feature still matters here, keep only the one-line outcome or the durable rule it established.
